---
apiVersion: v1
kind: Pod
metadata:
    name: nginx-resourcerequest-pod
spec:
    containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 8080
      resources:
        requests:
          memory: "100Mi"
          cpu: 0.5